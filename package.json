{
  "name": "ai-repo-assistant",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "eval": "pnpm --filter @ara/backend eval"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
